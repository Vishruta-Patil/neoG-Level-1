// Return an array with strings which have vowels

const str = ["abcd", "vishruta", "jklmn", "papaya, oven"];

const output = str.filter((i) => i.includes("a") || i.includes("e") || i.includes("i") || i.includes("o") || i.includes("u"));

console.log(output);
